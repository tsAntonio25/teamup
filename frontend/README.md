# Frontend

Frontend Folder of the Project  

## Dependencies used

* Angular

* TailwindCSS

* Auto Prefixer

* ChartJS

To install the following dependencies, run the following command inside the folder  

```bash
npm i angular tailwindcss @tailwindcss/postcss postcss autoprefixer chartjs
```

To run the server, run the following command:

```bash  
ng serve
```
